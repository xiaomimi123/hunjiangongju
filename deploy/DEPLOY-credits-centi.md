# 积分精度升级（0.01 最小单位）部署清单

> 迁移 `20260921000000_credits_centi` 把全库积分字段一次性 ×100（改为「0.01 积分」整数单位），
> 显示层统一 /100。**迁移不可重复执行**（migrate 服务按迁移记录保证只跑一次，勿手工重放该 SQL）。

## 部署前

务必备份数据库（涉及全表 UPDATE users/credit_logs/coze_tools/…）：

```bash
$C exec -T postgres pg_dump -U mixcut mixcut > ~/backup_before_centi_$(date +%F_%H%M).sql
```

## 部署后验收

1. 学员端余额显示不变（如原 22 分仍显示 22）；库内查询应为 2200；
2. 后台「系统设置」出现「积分定价」卡：视频单价默认 1，可改小数（如 0.5）；
3. 「模型配置→实拍生图」高级参数改 `{"pricePerImage":0.08}` → 学员生图 1 张扣 0.08、2 张扣 0.16；
4. 扣子工具编辑弹窗价格框可填小数（如 0.5）；
5. 后台给学员充值可输小数（如 10.5）；
6. 学员积分明细出现消费/退分流水（生成视频/实拍生图/工具·xxx）。
