# v2.2 P1 生成流水线 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 跑通生成流水线，从一个手工建的框架产出一条**真实可下载的 mp4**（真 HyperFrames 模板 + 真字幕对齐 + 真 ffmpeg 混音；P1 用 mock 图/音即可端到端验证，接真 key 后即真素材）。

**Architecture:** 沿用 monorepo。P0 已交付：全部新 Prisma 模型、AI 适配层（`llmComplete`/`imageGenerate`/`ttsSynthesize`/`asrTranscribe`，mock 模式）、`renderState`（transitionRender/setGenerationStatus/writeQc）。P1 在 worker 增加**第二条 BullMQ 队列 `generation`** 与 7 个 job，自研一套 HyperFrames 书单号模板，新增 `/admin/generate`+`/edit`+`/admin/bgm` 页。仍**纯增量**，不动旧混剪。

**Tech Stack:** TypeScript、BullMQ、fluent-ffmpeg + 系统 ffmpeg（已在镜像内）、HyperFrames（`npx hyperframes@0.7.33`，作为 worker 依赖预装）、Next.js App Router、vitest。

**参考文档（实现期必读）：**
- `docs/superpowers/references/hyperframes-reuse-reference.md` —— HyperFrames 模板结构/GSAP 时间线契约、silencedetect 算法（`noise=-35dB:d=0.18` → 反转 → coalesce 到 N 段）、最终 ffmpeg 混音滤镜链（`amix normalize=0` + `alimiter=0.95` + `loudnorm=I=-14:TP=-1:LRA=7`）、文案校验逻辑、我们的 `body-data.json` 数据契约与到 Prisma 模型的映射。
- `docs/superpowers/specs/2026-07-16-video-deconstruct-generate-v2.2-design.md` —— 总设计（§5 状态机、§6 jobs、§8 文案校验）。
- book-video 源码（只读参考，勿 fork）：clone 在 scratchpad，或 https://github.com/Endless1936/book-video。

## Global Constraints

- **纯增量**：不改/不删旧 `Material`/`Script`/`Task`/tags 相关模型、接口、页面、`worker/src/index.ts` 的旧 `pipeline` 队列。新队列/新 job/新页面并存。`next build` 与 `vitest` 始终通过。
- **文生图风格**：仅做**固定风格前后缀**——`generate-image` 每段把 `copy_frameworks.imageStylePrompt`（前后缀，可含 `{scene}` 占位）拼到段落场景描述上；不做参考图锚点（Phase 后）。
- **mock 优先出片**：P1 全流程在 `AI_MOCK=1` 或能力未启用时用 mock（合法 PNG/静音 WAV）跑通，产出真实 mp4（占位画面）。真实素材靠运营在模型配置页启用能力 + 填真 key。
- **HyperFrames 版本固定 `0.7.33`**，作为 `worker` 依赖预装（不靠运行时 npx 拉包，避免国内/离线失败）。渲染画布 **720×960 @30fps**（竖屏）。
- **时间单位**：Prisma 存毫秒（`body_timings`/segments），发给 GSAP 时间线时 `/1000` 转秒。
- **迁移**：`migrate deploy` + 手写 SQL；两容器都 `prisma generate`。构建 `NODE_ENV=production`；`DOCKER_BUILDKIT=0`。
- **每个涉及 web 的任务**结束跑生产构建门禁（EXIT 0）后 `rm -rf web/.next && docker compose restart web` 恢复 dev。
- **状态机**：生成阶段用 `setGenerationStatus`（generation_tasks.status，见 spec §5.2）；合成阶段创建 `RenderTask` 用 `transitionRender`。job 失败 → 置对应 `FAILED` + note。

## 文件结构（P1 新增/改动）

- Modify: `packages/db/prisma/schema.prisma` —— CopyFramework 加 `imageStylePrompt`（Task 1）
- Create: migration `<ts>_p1_image_style_prompt`（Task 1）
- Create: `packages/db/src/genQueue.ts` —— `enqueueGen(name, payload)` + `GenJobName`（Task 1）
- Modify: `packages/db/src/index.ts` —— 导出 genQueue（Task 1）
- Create: `worker/src/gen/index.ts` —— 第二个 Worker（监听 `generation` 队列）+ 分发（Task 1，各 job 增量接入）
- Modify: `worker/src/index.ts` —— 在同进程启动 gen worker（Task 1）
- Create: `worker/src/gen/generateScript.ts`（Task 2）+ `packages/db/src/scriptPolicy.ts`（校验，Task 2）+ 测试
- Create: `worker/src/gen/generateImage.ts`（Task 3）
- Create: `worker/src/gen/generateTts.ts`（Task 4）
- Create: `worker/src/gen/alignCaptions.ts`（Task 5）+ `packages/db/src/silence.ts`（纯算法，Task 5）+ 测试
- Create: `worker/templates/booklist/index.html.ts`（模板 codegen 函数）+ `worker/templates/booklist/package.json`（Task 6）
- Create: `worker/src/gen/renderVisuals.ts`（Task 6）
- Create: `worker/src/gen/renderVideo.ts`（Task 7，ffmpeg 混音）
- Create: `worker/src/gen/runGenQc.ts`（Task 7，复用质检思路，写 render_qc_reports）
- Create: web APIs `web/app/api/generate/*`、`web/app/api/frameworks/*`、`web/app/api/bgm/*`（Task 8/9）
- Create: pages `web/app/admin/generate/page.tsx`、`web/app/admin/generate/[id]/page.tsx`、`web/app/admin/generate/[id]/edit/page.tsx`、`web/app/admin/bgm/page.tsx`（Task 8/9）+ 侧栏入口
- Modify: `worker/package.json` —— 加 `hyperframes@0.7.33` 依赖（Task 6）

---

### Task 1: image_style_prompt 迁移 + 第二队列 `generation` + gen worker 骨架

**Files:** schema.prisma、新迁移、`packages/db/src/genQueue.ts`、`packages/db/src/index.ts`、`worker/src/gen/index.ts`、`worker/src/index.ts`

**Interfaces:**
- Produces: `enqueueGen(name: GenJobName, payload: { genTaskId?: string; renderTaskId?: string })`；`GenJobName = 'generate-script'|'generate-image'|'generate-tts'|'align-captions'|'render-visuals'|'render-video'|'run-gen-qc'`；第二个 BullMQ Worker 监听队列 `generation`，dispatch 到各 job（P1 各任务逐个填实现，本任务先建骨架 + 占位抛"未实现"）。`CopyFramework.imageStylePrompt`。

- [ ] **Step 1: schema 加字段**：CopyFramework 追加 `imageStylePrompt String? @map("image_style_prompt")`。
- [ ] **Step 2: 迁移**：`packages/db/prisma/migrations/<ts>_p1_image_style_prompt/migration.sql`：
```sql
ALTER TABLE "copy_frameworks" ADD COLUMN "image_style_prompt" TEXT;
```
- [ ] **Step 3: genQueue.ts**：
```ts
import { Queue } from 'bullmq'
import { redisConnection } from './queue'

export type GenJobName =
  | 'generate-script' | 'generate-image' | 'generate-tts'
  | 'align-captions' | 'render-visuals' | 'render-video' | 'run-gen-qc'

let q: Queue | null = null
function genQueue(): Queue {
  if (!q) q = new Queue('generation', { connection: redisConnection })
  return q
}
export async function enqueueGen(name: GenJobName, payload: { genTaskId?: string; renderTaskId?: string }): Promise<void> {
  await genQueue().add(name, payload, { removeOnComplete: 100, removeOnFail: 500 })
}
```
从 `index.ts` 追加 `export { enqueueGen } from './genQueue'; export type { GenJobName } from './genQueue'`。
- [ ] **Step 4: gen worker 骨架** `worker/src/gen/index.ts`：
```ts
import { Worker, type Job } from 'bullmq'
import { prisma, redisConnection, setGenerationStatus, transitionRender } from '@mixcut/db'

async function dispatch(job: Job): Promise<void> {
  console.log(`[gen] ${job.name}`, job.data)
  switch (job.name) {
    // 各 job 由后续任务接入；未实现先抛错，避免静默
    default: throw new Error(`未接入 gen job: ${job.name}`)
  }
}

export function startGenWorker() {
  const w = new Worker('generation', dispatch, { connection: redisConnection, concurrency: 2 })
  w.on('completed', (j) => console.log(`[gen] ${j.name} done`))
  w.on('failed', async (j, err) => {
    console.error(`[gen] ${j?.name} failed: ${err.message}`)
    const d = (j?.data ?? {}) as { genTaskId?: string; renderTaskId?: string }
    try {
      if (d.renderTaskId) await transitionRender(d.renderTaskId, 'FAILED', `${j?.name}: ${err.message}`).catch(() => {})
      else if (d.genTaskId) await setGenerationStatus(d.genTaskId, 'FAILED').catch(() => {})
    } catch {}
  })
  console.log('[gen] generation worker started')
  return w
}
```
- [ ] **Step 5: 同进程启动**：`worker/src/index.ts` 末尾加 `import { startGenWorker } from './gen'; startGenWorker()`。
- [ ] **Step 6: 应用迁移 + generate（web+worker）+ 重启 worker + tsc**。验证 worker 日志出现 `[gen] generation worker started`。
- [ ] **Step 7: Commit** `feat(gen): image_style_prompt 迁移 + generation 队列与 gen worker 骨架`。

---

### Task 2: generate-script（LLM 生成文案 + 长度校验循环）

**Files:** `packages/db/src/scriptPolicy.ts`（+测试）、`worker/src/gen/generateScript.ts`、`worker/src/gen/index.ts`（接入）

**Interfaces:**
- Consumes: `llmComplete`（P0）、`getCapabilityConfig`、framework(`frameworkText`/`suggestedSegmentCount`/`maxLines`/`maxTotalChars`)、`GenerationTask`(`subject`/`variables`)、`setGenerationStatus`、`enqueueGen`
- Produces: `validateScript(lines: string[], maxLines?: number, maxTotalChars?: number): { lines:number; chars:number; errors:string[] }`；job 写 `generated_segments`（按行分段）后置 `IMAGE_GENERATING` 并 `enqueueGen('generate-image',{genTaskId})`。

- [ ] **Step 1: scriptPolicy.ts（纯函数，参考 reference §5）**：
```ts
export function countChars(lines: string[]): number {
  return Array.from(lines.join('')).length // code points，CJK 正确
}
export function validateScript(lines: string[], maxLines = 21, maxTotalChars = 220) {
  const clean = lines.map((l) => l.trim()).filter(Boolean)
  const chars = countChars(clean)
  const errors: string[] = []
  if (clean.length > maxLines) errors.push(`正文最多 ${maxLines} 行，当前 ${clean.length}`)
  if (chars > maxTotalChars) errors.push(`正文最多 ${maxTotalChars} 字，当前 ${chars}`)
  return { lines: clean.length, chars, errors, clean }
}
```
- [ ] **Step 2: 测试 scriptPolicy.test.ts**：CJK 计数正确；超行/超字各报错；干净文案通过。运行通过。
- [ ] **Step 3: generateScript.ts**：构造 prompt（framework.frameworkText + subject + variables + 「分 N 段、每段一行、总字数≤max、严禁照搬原文」），调 `llmComplete`；按行拆分→`validateScript`（阈值取 framework.maxLines/maxTotalChars，缺省 21/220）；超限则把「过长，请压缩到 N 行/M 字以内重写」追加再调，最多 3 次；仍超 → `setGenerationStatus('FAILED')` 抛错；通过则事务写 `generated_segments`（seqNo 从 1，scriptText=行），`setGenerationStatus('IMAGE_GENERATING')`，`enqueueGen('generate-image',{genTaskId})`。job 入口从 `SCRIPT_GENERATING` 起（发起时已置）。
- [ ] **Step 4: 接入 dispatch**：`case 'generate-script': return generateScript(job.data.genTaskId)`。
- [ ] **Step 5: 冒烟**（mock）：手工 SQL 插一个 framework + generation_task(status=SCRIPT_GENERATING)，`enqueueGen`，看 worker 日志 + `generated_segments` 有行 + status=IMAGE_GENERATING（generate-image 未实现会在下一 job 抛错，正常）。
- [ ] **Step 6: Commit**。

---

### Task 3: generate-image（逐段文生图 + 风格前后缀）

**Files:** `worker/src/gen/generateImage.ts`、dispatch 接入

**Interfaces:**
- Consumes: `imageGenerate`（P0，mock 返回 PNG）、framework.imageStylePrompt、`generated_segments`、`DATA_DIR`/`urlToAbs`（worker/src/paths.ts）、`setGenerationStatus`、`enqueueGen`
- Produces: 每段写 `generated_segments.image_url = /api/files/gen/<genTaskId>/<seqNo>.png`，文件落 `/data/gen/<genTaskId>/<seqNo>.png`；完成置 `TTS_GENERATING` + `enqueueGen('generate-tts')`。

- [ ] **Step 1**：读该任务所有段，逐段：`prompt = [imageStylePrompt前缀, scriptText, imageStylePrompt后缀].join()`（P1 简单拼接：`(framework.imageStylePrompt ?? '') + ' ' + scriptText`）；`const png = await imageGenerate({ prompt, size: '720x960' })`；`fs.writeFile('/data/gen/<id>/<seq>.png', png)`；更新 image_url。
- [ ] **Step 2**：全部完成 → `setGenerationStatus('TTS_GENERATING')` + `enqueueGen('generate-tts',{genTaskId})`。dispatch 接入。
- [ ] **Step 3: 冒烟**（承接 Task 2 的任务）：跑通后 `/data/gen/<id>/` 有 N 个 png（mock 下是 1x1 PNG）、image_url 已写、status=TTS_GENERATING。
- [ ] **Step 4: Commit**。

---

### Task 4: generate-tts（整篇一次配音）

**Files:** `worker/src/gen/generateTts.ts`、dispatch 接入

**Interfaces:**
- Consumes: `ttsSynthesize`（P0，mock 返回静音 WAV）、`generated_segments`、`setGenerationStatus`、`enqueueGen`
- Produces: 整篇文案拼接一次配音，落 `/data/gen/<id>/full_audio.wav`（mock）/`.mp3`（真实），写 `generation_tasks.full_audio_url`；置 `CAPTION_ALIGNING` + `enqueueGen('align-captions')`。

- [ ] **Step 1**：拼全文 `segments.map(s=>s.scriptText).join('\n')` → `ttsSynthesize({text})` → 写文件（按返回判断扩展名：mock 是 WAV；真实按 content 存 `.mp3`，P1 统一存 `.wav` 亦可，后续 ffmpeg 兼容）。写 full_audio_url。
- [ ] **Step 2**：`setGenerationStatus('CAPTION_ALIGNING')` + `enqueueGen('align-captions')`。dispatch 接入。
- [ ] **Step 3: 冒烟** + **Commit**。

---

### Task 5: align-captions（silencedetect → body_timings）

**Files:** `packages/db/src/silence.ts`（纯算法 + 测试）、`worker/src/gen/alignCaptions.ts`、dispatch 接入

**Interfaces:**
- Consumes: full_audio_url、段数 N、ffmpeg（silencedetect）、`setGenerationStatus`
- Produces: `parseSilence(output): {type,time}[]`、`buildSpeech(duration, events): {start,end}[]`、`coalesce(segs, target): {start,end}[]`、`buildTimings(n, segs, skipLeading): {seqNo,startMs,endMs}[]`（reference §3 算法）；job 跑 `ffmpeg -i <audio> -af silencedetect=noise=-35dB:d=0.18 -f null -`，解析→写 `generation_tasks.body_timings`；置 `ASSET_READY`（**停在此，等运营确认合成**，不自动进 render）。

- [ ] **Step 1: silence.ts 纯函数**（按 reference §3 步骤 4–7 实现：正则 `/silence_(start|end):\s*([0-9.]+)/g`；反转出 speech（丢 <0.08s）；贪心合并最小间隔到 `target=N+skipLeading`；`slice(skipLeading, skipLeading+N)`，数量不符抛错；返回 ms）。
- [ ] **Step 2: 测试 silence.test.ts**：给一段构造的 silencedetect 输出 + duration + N，断言得到 N 个 `{seqNo,startMs,endMs}` 且单调递增；数量不符时抛错。
  - ⚠️ mock 静音 WAV **全静音**→silencedetect 无 speech 段。为让 mock 闭环不卡：alignCaptions 里若解析出的 speech 段数 < N（典型 mock 全静音场景），**退化为按音频总时长均分 N 段**（`ffprobe` 取时长，均匀切）。这是 mock/兜底路径，真实配音走 silencedetect 主路径。测试要覆盖「正常 silencedetect」与「兜底均分」两条。
- [ ] **Step 3: alignCaptions.ts**：ffprobe 取时长；跑 silencedetect 抓输出；主路径 silence 算法；不足则均分兜底；写 body_timings（`[{seqNo,startMs,endMs}]`）；`setGenerationStatus('ASSET_READY')`。dispatch 接入。**不** enqueue 下一步。
- [ ] **Step 4: 冒烟**：mock 音频跑到 `ASSET_READY` + body_timings 有 N 段。**Commit**。

---

### Task 6: 自研 HyperFrames 书单号模板 + render-visuals

**Files:** `worker/templates/booklist/package.json`、`worker/templates/booklist/indexHtml.ts`（codegen 函数）、`worker/src/gen/renderVisuals.ts`、`worker/package.json`（加 hyperframes 依赖）、dispatch 接入

**Interfaces:**
- Consumes: `generated_segments`(image_url/scriptText/seqNo)、body_timings、`copy_frameworks.overlayTemplate`+`generation_tasks.variables`（填标题卡/水印）、`renderTemplate`（模板标识，P1 固定 `booklist`）
- Produces: `buildBodyData(task): BodyData`、`renderIndexHtml(data): string`（自研模板，见下）；job：创建 `RenderTask`(status VISUAL_RENDERING)、拷模板到 `/data/gen/<id>/hf/`、拷图到 `hf/media/`、写 codegen 的 `index.html`、跑 `npx hyperframes@0.7.33 render --quality standard --output renders/body.mp4`、`transitionRender(rt,'RENDERING')` + `enqueueGen('render-video',{renderTaskId})`。

- [ ] **Step 1: 预装 hyperframes**：`worker/package.json` deps 加 `"hyperframes": "0.7.33"`；`docker compose exec worker sh -lc 'cd /app && npm install'`；验证 `docker compose exec worker sh -lc 'npx hyperframes@0.7.33 --version'` 可用（国内镜像已配 npmmirror；若拉不到，记录到 report，属环境待办）。
- [ ] **Step 2: 模板 codegen** `worker/templates/booklist/indexHtml.ts`（自研，reference §1/§6 契约）：导出 `renderIndexHtml(data: BodyData): string`，生成自包含 HTML：
  - `<main data-composition-id="main" data-start="0" data-duration="<lastEnd s>" data-width="720" data-height="960">`
  - **常驻标题卡**（顶部，`overlay.title` 书名 + `overlay.subtitle`，`data-layout-ignore`，opacity 1 常驻）
  - **常驻水印**（底部，`overlay.watermark`，`data-layout-ignore`）
  - **图片轮播**：每段一个 `.scene.sN` 绝对定位 `.photo`（background-image=对应图，`inset:-22px;background-size:cover`）
  - **字幕层**：每段一个 `.cap.sN`
  - GSAP（CDN gsap@3.14.2）paused timeline `window.__timelines["main"]`：每段做 缓推近（scale 1.035→1.105，dur=段长+1.2，sine.inOut，pos=段 start s）+ crossfade（下一段 opacity 0→1、当前 0，dur 0.72，pos=下一段 start）+ 字幕 revealCaption（fromTo opacity/y 于 start，`set opacity:0` 于 end）。
  - 复制 reference §1「animation techniques」的 tween 写法，时间用 `startMs/1000`。
- [ ] **Step 3: package.json**（`worker/templates/booklist/package.json`）：`{"private":true,"type":"module","scripts":{"render":"npx --yes hyperframes@0.7.33 render"}}`。
- [ ] **Step 4: renderVisuals.ts**：`buildBodyData`（组装 overlay 从 overlayTemplate+variables 占位替换、images 从各段 image_url 拷 media、segments 从 body_timings+scriptText join）→ 写 index.html → 拷图 → spawn `npx hyperframes@0.7.33 render --quality standard --output renders/body.mp4`（cwd=hf 目录）→ 校验 `renders/body.mp4` 存在且 720×960（ffprobe）→ `transitionRender(rt,'RENDERING')` + `enqueueGen('render-video')`。RenderTask 由本 job 创建（generationTaskId、status VISUAL_RENDERING）。
- [ ] **Step 5: 冒烟**：承接 ASSET_READY 的任务，手动 `enqueueGen('render-visuals',{genTaskId})`；产出 `renders/body.mp4`（720×960，mock 图是纯色块）。若 hyperframes 拉包失败，记录并把该冒烟标记为环境阻塞（其余代码仍须 tsc/lint 通过）。
- [ ] **Step 6: Commit**。

---

### Task 7: render-video（ffmpeg 混音）+ run-gen-qc

**Files:** `worker/src/gen/renderVideo.ts`、`worker/src/gen/runGenQc.ts`、dispatch 接入

**Interfaces:**
- Consumes: RenderTask、`renders/body.mp4`、full_audio_url、可选 BGM（render_tasks.bgmId→bgm_library.fileUrl）、`transitionRender`/`writeQc`、worker/src/ffmpeg 的 `detectBlack`/`detectSilence`
- Produces: `render-video`：ffmpeg 合视觉+配音+BGM+loudnorm（reference §4）→ `/data/gen/<id>/final.mp4`，写 `render_tasks.video_url`+`subtitle_url`（顺带生成 SRT）；`transitionRender('PREVIEW_PENDING')`。运营确认预览 → API 触发 `run-gen-qc`：`transitionRender('QC_RUNNING')`→黑屏/静音/字幕越界（写 render_qc_reports）→ 通过 `QC_PASSED`+`EXPORTED`，否则 `QC_FAILED`。

- [ ] **Step 1: renderVideo.ts**：ffmpeg 输入 `[0]=body.mp4 [1]=full_audio [2]=bgm(可选,-stream_loop -1)`；音频链（无 intro/sfx，简化版 reference §4）：`[1:a]aresample=48000,volume=1.0[voice]; [2:a]atrim=0:<dur>,aresample=48000,volume=0.32[bgm]; [voice][bgm]amix=inputs=2:duration=first:normalize=0,alimiter=limit=0.95,loudnorm=I=-14:TP=-1:LRA=7,aformat=...[a]`（无 BGM 时只 voice→loudnorm）；`-map 0:v -map [a] -c:v libx264 -pix_fmt yuv420p -profile:v high -c:a aac -b:a 192k -movflags +faststart -shortest`。SRT 从 body_timings+scriptText 生成（复用现有 `buildSrt` 思路）。ffprobe 校验 720×960 + 有音轨。`transitionRender('PREVIEW_PENDING')`。dispatch 接入 `render-video`。
- [ ] **Step 2: runGenQc.ts**：`transitionRender('QC_RUNNING')`；`detectBlack`/`detectSilence`（现有 worker/src/ffmpeg 导出）跑 final.mp4；字幕越界用 body_timings 段长 vs scriptText 长度（复用 `checkSubtitleOverflow` 思路）；各写 `writeQc(rt, type, pass|fail, detail)`；全过 `transitionRender('QC_PASSED')`→`transitionRender('EXPORTED')`，否则 `QC_FAILED`。dispatch 接入 `run-gen-qc`。
  - ⚠️ mock 音频全静音→`detectSilence` 必报静音。mock 场景 QC 允许「静音」为 warn 不 fail（真实配音才严格）；用 `AI_MOCK` 或音频是否 mock 判定。记录在 detail。
- [ ] **Step 3: 冒烟**：承接 render-visuals 的 RenderTask → `render-video` 产出 `final.mp4`（720×960，有音轨），status=PREVIEW_PENDING；再手动 `enqueueGen('run-gen-qc',{renderTaskId})` → EXPORTED（mock 静音判 warn）。**Commit**。

---

### Task 8: 生成 API + `/admin/generate`（发起 + 进度 + 单段重生）

**Files:** `web/app/api/frameworks/route.ts`+`[id]/route.ts`、`web/app/api/generate/route.ts`+`[id]/route.ts`+`[id]/segments/[segNo]/regenerate/route.ts`+`[id]/render/route.ts`、`web/app/admin/generate/page.tsx`+`[id]/page.tsx`、侧栏

**Interfaces:**
- Consumes: prisma、`enqueueGen`、`setGenerationStatus`、`requireRole('operator')`、`handler`
- Produces:
  - `GET /api/frameworks`（列表）、`GET/PATCH /api/frameworks/[id]`（查看/编辑框架，含 imageStylePrompt/overlayTemplate/max*）
  - `POST /api/generate`（body `{frameworkId,subject,variables}`→建 generation_task(SCRIPT_GENERATING)+`enqueueGen('generate-script')`→返回 id）
  - `GET /api/generate/[id]`（任务+segments+body_timings+renderTasks）
  - `POST /api/generate/[id]/segments/[segNo]/regenerate`（重跑该段文生图；ASSET_READY 后允许）
  - `POST /api/generate/[id]/render`（ASSET_READY→`enqueueGen('render-visuals')`）
  - 页面：`/admin/generate` 选框架+填 subject/variables 发起 + 任务列表；`/admin/generate/[id]` 进度（生产线/状态）+ 分段（图+文案）预览 + 单段重生 + 「确认合成」按钮（调 render）。

- [ ] Steps：建各路由（仿现有 `/api/tasks` 风格）；建两页（复用 PageHeader/card/StatusPill，3s 轮询 like 学员 works 详情）；侧栏「生成」组加入口；tsc + 生产构建门禁 + 截图；Commit。（每步含具体命令与验收：发起→轮询到 ASSET_READY→点确认合成→轮询到 PREVIEW_PENDING/EXPORTED。）

---

### Task 9: `/admin/generate/[id]/edit`（轻量编辑）+ `/admin/bgm` 曲库

**Files:** `web/app/api/generate/[id]/segments/[segNo]/route.ts`(PATCH 换图/改字幕)、`web/app/api/generate/[id]/bgm/route.ts`、`web/app/api/bgm/route.ts`(GET/POST/DELETE)、`web/app/admin/generate/[id]/edit/page.tsx`、`web/app/admin/bgm/page.tsx`、侧栏

**Interfaces:**
- Produces：
  - `PATCH /api/generate/[id]/segments/[segNo]`（改 scriptText / 上传替换 image；改后需重跑对齐——置回 `TTS_GENERATING` 或仅 `CAPTION_ALIGNING` 视改动，简单起见改字幕→重跑 align，换图→仅重渲不改时轴）
  - `POST /api/generate/[id]/bgm`（设 render 用哪首 BGM，存到将来 RenderTask.bgmId 或任务级）
  - `GET/POST/DELETE /api/bgm`（曲库，上传 mp3 到 `/data/bgm/`，style_tag）
  - `/admin/generate/[id]/edit`：换图/改字幕/换BGM/调序 UI；`/admin/bgm`：曲库 CRUD（仿素材页但简单）。

- [ ] Steps：建路由 + 两页 + 侧栏入口；改动后重对齐的最小实现；tsc + 生产构建 + 截图；Commit。

---

### Task 10: 端到端 mock 验证（里程碑）

**Files:** 无（验证 + seed 一个演示框架）

- [ ] **Step 1**：写一个最小 seed/SQL 建一个演示框架（frameworkText 简述、suggestedSegmentCount=8、maxLines/maxTotalChars 合理、imageStylePrompt 一段风格词、overlayTemplate `{title_card:"{{标题}} {{副标题}}",watermark:"{{账号}}"}`、renderTemplate='booklist'、visualStyleType='ai_illustration'）。
- [ ] **Step 2**：运营登录 `/admin/generate` → 选该框架 + subject「活下去的理由」+ variables → 发起。
- [ ] **Step 3**：轮询走完 SCRIPT→IMAGE→TTS→ALIGN→ASSET_READY；分段页看到 8 段文案 + 占位图。
- [ ] **Step 4**：点「确认合成」→ VISUAL_RENDERING→RENDERING→PREVIEW_PENDING，能在详情页播放 `final.mp4`（720×960，标题卡+水印+图轮播+字幕，占位画面）。
- [ ] **Step 5**：跑 QC → EXPORTED；能下载 mp4 + SRT。
- [ ] **Step 6**：全套 `npm test` 绿 + `NODE_ENV=production next build` EXIT 0 + 截图关键页。清理演示数据（保留 seed 框架可选）。记录到 ledger。若 hyperframes 因网络未能真渲，明确标注「代码就绪，待渲染环境」并给出用户侧验证步骤。

---

## P1 验收标准
1. 生成流水线 7 job 全部接入 `generation` 队列，dispatch 无「未接入」抛错。
2. mock 模式下端到端产出真实 `final.mp4`（720×960、有音轨、标题卡/水印/图轮播/字幕），并通过（或 warn）质检至 `EXPORTED`；可下载 mp4+SRT。（hyperframes 若被网络阻塞，代码与冒烟其余环节须过，渲染环节标注环境待办。）
3. `/admin/generate`+`/[id]`+`/edit`+`/admin/bgm` 可用；发起/进度/单段重生/确认合成/换图改字幕/BGM 均通。
4. `npm test` 全绿（P0 26 + scriptPolicy + silence 新测试）；生产构建 EXIT 0。
5. 纯增量：旧混剪与 P0 功能不受影响。
6. 文案长度校验：超限自动重写、3 次上限失败置 FAILED（不放行）。

## 交接 P2（拆解流水线）
- 复用：AI 适配层（asr/llm）、renderState、genQueue 模式、silence/ffmpeg helper。
- P2 新增：`download-douyin`+上传兜底、`transcribe`、`detect-scenes`、`extract-framework`（产框架，含估算 maxLines/maxTotalChars/imageStylePrompt）、`/admin/extract`+`/admin/frameworks`。

## 待确认（不阻塞 P1 mock，接真出片前）
- aitoken.homes 四能力真实端点/鉴权是否 OpenAI 兼容（§15#1 已按兼容默认，接真前抽验 llm/image/tts 各一次）。
- hyperframes@0.7.33 在生产/dev 容器可 `npx` 拉取（国内 npmmirror）——Task 6 预装为依赖以规避。
- 文案阈值（maxLines/maxTotalChars）默认 21/220，客户真实参考视频校准留 P2 `extract-framework`。
