# 书单快闪版字体

客户字体放这里(self-contained @font-face 内嵌渲染)：
- `title.ttf` —— 书名/开场标题(源工程用 字由玄真 / 三极极宋超粗新)
- `sub.otf` —— 口播字幕(源工程用 莫雪体)

缺失时渲染自动回退系统字体，不报错。
映射见 renderVisuals: flash-title→title.ttf, subtitle→sub.otf。
